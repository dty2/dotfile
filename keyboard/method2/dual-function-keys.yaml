- JOB: "intercept -g $DEVNODE | dual-function-keys -c /etc/interception/caps2ctrlesc.yaml | uinput -d $DEVNODE"
  DEVICE:
    NAME: "AT Translated Set 2 keyboard" # 任务作用的设备
- JOB: "intercept -g $DEVNODE | dual-function-keys -c /etc/interception/caps2ctrlesc.yaml | uinput -d $DEVNODE"
  DEVICE:
    NAME: "BLE Keyboard       Keyboard"
